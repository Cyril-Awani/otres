export const metadata = {
	title: 'Categories - Discover Our Menu',
	description:
		'Browse through our delicious categories including rice, swallow, soups, proteins, sides, combo dishes, and breakfast menu.',
};

export default function CategoriesLayout({
	children,
}: {
	children: React.ReactNode;
}) {
	return <>{children}</>;
}
